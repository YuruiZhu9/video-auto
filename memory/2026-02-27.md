# Memory — 2026-02-27

## 今日主要进展（会话紧凑前备份）

### 完成事项
- DingTalk 插件配置完成，clientId: `dingod3nfto6gfbbrehc`，用户 chat_id: `03003745585526383319`
- 创建三个 AI Agent 提示文件：
  - `/workspace/agents/info-fetcher-prompt.md` — 信息抓取助手（AIbase + TechCrunch + 36kr）
  - `/workspace/agents/tech-analyst-prompt.md` — 技术前沿分析师
  - `/workspace/agents/business-analyst-prompt.md` — 商业需求洞察分析师（含长期记忆）
- 创建 4 个定时 cron job：
  - `b663647c` — 信息抓取助手 09:00 CST 每日
  - `b27b84b5` — 信息抓取助手 18:00 CST 每日
  - `fc435f87` — 技术前沿分析师 11:30 CST 每日
  - `29c8ba5c` — 技术前沿+商业洞察 20:00 CST 每日
- 所有三个 agent 测试运行成功，报告已发送至钉钉
- 商业洞察长期记忆初始化：`/workspace/memory/business-insights.md`
- USER.md、IDENTITY.md 已填写（名字：小M）
- `web_search` 和 `web_fetch` 工具已从 deny list 移除并启用
- TOOLS.md 写入了博查 AI Web Search API 密钥和调用示例

### 待解决问题（blocked）
- **博查 AI web_search 集成未完成**
  - 用户提供了博查 AI Web Search API key: `sk-7aa8fbfa43534a9e8fb26a3d1ab74b6a`
  - OpenClaw 的 `tools.web.search.provider` 只接受 enum: `brave | perplexity | grok`，不支持 "bocha"
  - 尝试过用 `provider: "bocha"` 调用 `gateway(action="config.patch")` → 返回 `"invalid config"` 错误
  - 计划用 `provider: "perplexity"` + `baseUrl: "https://api.bochaai.com/v1"` 绕过，但尚未完成
  - **下次会话优先处理此项**
  - 博查 AI 文档: https://open.bochaai.com
  - 博查 AI Web Search 接口: `POST https://api.bochaai.com/v1/web-search`（Bing 兼容 JSON 格式）
  - 当前 workaround（非 web_search tool）：直接用 `exec + curl` 调用博查 API（已在 TOOLS.md 记录）

### 关键路径文件
- 配置文件: `/root/.openclaw/openclaw.json`
- 报告目录: `/workspace/reports/`（命名规则: `info-YYYY-MM-DD-HH.md`, `tech-YYYY-MM-DD-HH.md`, `business-YYYY-MM-DD.md`）
- 长期记忆: `/workspace/memory/business-insights.md`

### 用户背景
- 想转行做**推荐系统算法工程师**，持续关注大模型发展
- 对 AI 商机和独立开发感兴趣
- 在钉钉私聊中联系我
- 无境外支付手段（不能用 Brave/Perplexity 官方 key）

### 技术注意事项
- `cron.run` action 容易 timeout（Gateway WebSocket 问题），用 `sessions_spawn` 代替触发测试
- Server 环境屏蔽 YouTube 和 Twitter/X，所以 agent 用 AIbase + TechCrunch + 36kr
- 钉钉 dingtalk.capabilities.inlineButtons 未启用（不支持按钮）

# OpenClaw 配置周刊 - 2026-03-28

## 📖 官方文档更新

### v2026.3.24 发布：体验优化稳定版

本周（3月24日）发布 v2026.3.24，聚焦四大方向：

| 功能 | 详情 |
|------|------|
| **OpenAI API 兼容** | 新增 `/v1/models` + `/v1/embeddings` 端点，RAG 应用可直接用 OpenAI SDK 调用 |
| **工具可见性** | `/tools` 命令显示实际可用工具（而非所有声明工具） |
| **Microsoft Teams** | 官方 SDK 升级，支持流式回复、welcome 卡片、typing 指示器 |
| **7个核心 Skill 一键安装** | coding-agent、gh-issues、weather 等：`openclaw skills install <name>` |

其他改进：
- CLI 新增 `--container` 参数（容器化部署更便捷）
- Discord `autoThreadName: "generated"` 自动命名
- Node 22.14+ 最低要求（相对放宽）
- 沙箱媒体访问修复（#54034）

### v2026.3.22/23 回顾（上周后续）

- **v2026.3.22**：重大架构升级，ClawHub 集成 + 模型默认 GPT-5.4
- **v2026.3.23**：紧急补丁（Web UI 打包遗漏、微信插件失效、Mistral 422 修复）

---

## 🔧 配置技巧

### ⚠️ Breaking Changes 速查（3月必改项）

**1. 环境变量全面重命名**
```bash
# 旧前缀 → 新前缀（立即修改 .env 文件！）
CLAWDBOT_*  →  OPENCLAW_*
MOLTBOT_*   →  OPENCLAW_*
```

**2. Chrome 扩展中继已移除**
```bash
# 旧版 driver: "extension" 废止
# 使用 openclaw doctor --fix 自动迁移
```

**3. 插件安装优先 ClawHub**
```bash
# 优先从 ClawHub 查找，无才回退 npm
openclaw plugins install <package>
# 指定市场：
openclaw plugins install clawhub:<pkg>
openclaw plugins install claude:<pkg>
```

**4. SDK 路径变更**
```bash
# 旧路径 → 新路径
openclaw/extension-api → openclaw/plugin-sdk/*
```

### Gateway 默认安全强化（新安装默认）

| 配置项 | 旧默认 | **新默认** |
|--------|--------|-----------|
| `gateway.bind` | `0.0.0.0` | **`127.0.0.1`** |
| `gateway.auth.enabled` | `false` | **`true`** |
| `gateway.auth.token` | 无 | **自动生成** |
| `execution.engine` | `docker` | `podman`（如有） |

### 快速安装 & 权限配置

```bash
# 安装
npm install -g openclaw@latest

# 启用完整权限
openclaw config set tools.profile coding
openclaw config set browser.enabled true
openclaw config set tools.allow '[]'

# 立即升级
openclaw update
openclaw security audit --deep
```

### Skill 一键安装（v2026.3.22+）

```bash
openclaw skills install agent-council     # 多 Agent 协调 ⭐
openclaw skills install coding-agent       # 编码助手
openclaw skills install tavily-search      # 实时搜索
openclaw skills install self-improving-agent
openclaw skills install agent-browser       # 浏览器自动化
```

---

## ❓ 常见问题与解决

### CVE-2026-25253（CVSS 9.8 紧急！）

**影响**：Gateway 远程代码执行漏洞，影响 v3.x 之前所有版本，攻击者通过暴露的 18789 端口执行任意代码。**正被积极利用中。**

**立即行动：**
```bash
openclaw --version          # 检查版本
npm install -g @openclaw/cli@latest   # 升级
openclaw doctor --security  # 验证修补状态
```

### ClawHavoc 恶意技能事件后续

| 指标 | 数值 |
|------|------|
| 已清除恶意技能 | 2,400+ |
| 已暴露实例（Bitdefender） | 135,000 |
| 已确认被黑实例 | 30,000+ |

**如果在 2025.10-2026.01 安装过不熟悉的技能，请：**
1. `openclaw skill list` 检查已安装技能
2. 移除可疑技能
3. **立即轮换所有 API Key**（估计数千个已泄露）
4. 检查 LLM 提供商账单异常

### 18789 端口扫描预警

CVE 披露后，针对 18789 端口的网络扫描活动暴增：
- 2025.12：80,000 个暴露实例
- 2026.02：135,000 个暴露实例（+69%）

**确认你的 Gateway 绑定到 `127.0.0.1`**，且防火墙封堵外部访问。

### ClawHub 安全新增

- **VirusTotal 扫描整合**：所有新上传技能自动扫描并显示结果
  ```bash
  openclaw skill virustotal <skill-name>
  openclaw skill report <skill-name> --reason "..."
  ```
- **技能签名验证（Beta）**：开发者可对技能进行数字签名
  ```bash
  openclaw skill install --signed-only <skill-name>  # 只装已签名
  openclaw skill info <skill-name> | grep signature
  ```

---

## 🚀 最佳实践

### 记忆系统 v2 预览（即将上线）

v2 版本预计引入：
- 向量化记忆搜索（取代纯文本匹配）
- 记忆分层（公开/私密/敏感）
- 自动脱敏
- 跨 Agent 共享记忆（opt-in）

```bash
# 开启预览
openclaw start --experimental-memory-v2
```

### Nvidia NemoClaw GPU 加速（GTC 2026 发布）

```bash
openclaw config set execution.gpu.enabled true
openclaw config set execution.gpu.runtime nvidia
```
支持：本地 LLM 推理（Ollama + CUDA）、browser-use GPU 渲染、Whisper 加速、向量记忆搜索。

### Tencent 中文生态套件（3月新开源）

- **微信官方 Adapter**：取代社区 WeChatFerry
- **中文 STT 模型**：比 Whisper 更准的中文语音识别
- **飞书 Lark Adapter**：企业级支持
- 中文 SOUL.md 模板（中文语境人格优化）

### 定时任务配置（当前使用方式确认）

```bash
# 推荐方式：CLI 创建
openclaw cron add --name "OpenClaw配置专家日报" \
  --cron "0 22 * * *" \
  --session isolated \
  --message "生成今日OpenClaw技术周报" \
  --announce --channel dingtalk \
  --to "03003745585526383319" \
  --tz "Asia/Shanghai"
```

### SubAgent 五种启用方式

1. 命令行 spawn（`/subagents spawn`）
2. AGENTS.md 规则触发
3. agentToAgent 工具跨体调用
4. Coding Team Setup v2.0（`claw skill install coding-team-setup@v2.0`）
5. 飞书多账户物理隔离

---

## 📅 本期总结

### 本周关键词
- **v2026.3.24**：OpenAI API 兼容端点上线 + 工具可见性改进
- **CVE-2026-25253**：CVSS 9.8 紧急，18789 端口漏洞正被积极利用，**立即升级**
- **ClawHub 安全升级**：VirusTotal 扫描 + 技能签名验证 Beta 上线
- **ClawawHavoc 后续**：2,400+ 恶意技能已清除，API Key 泄露建议轮换

### 本周行动清单

**今天（必须）：**
- [ ] `openclaw update` 升级到最新版本
- [ ] `openclaw config get gateway.bind` 确认绑定 `127.0.0.1`
- [ ] `openclaw security audit --deep` 做安全审计

**本周：**
- [ ] 检查 `.env` 中 `CLAWDBOT_*` / `MOLTBOT_*` → 替换为 `OPENCLAW_*`
- [ ] `openclaw doctor --fix` 修复 Chrome 扩展迁移问题
- [ ] `openclaw skill list` 审查已安装技能，移除可疑项
- [ ] 轮换所有 API Key（如曾安装第三方 Skill）

**可选尝鲜：**
- [ ] 体验 `openclaw start --experimental-memory-v2` 记忆 v2 预览
- [ ] 试用 `openclaw skills install --signed-only <skill>` 只装已签名技能

### 常用链接
| 资源 | 链接 |
|------|------|
| 官方文档 | https://docs.openclaw.ai/zh-CN |
| 中文站 | https://openclaw.cc/ |
| ClawHub | https://clawhub.com/ |
| 中文社区 | https://clawd.org.cn/ |
| GitHub | https://github.com/openclaw/openclaw |

---
*OpenClaw 配置周刊 · 2026-03-28 · 由 OpenClaw 配置专家 Agent 自动生成*

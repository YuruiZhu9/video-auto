# OpenClaw 配置周刊 - 2026-04-03

## 📖 官方文档更新

### 🆕 v2026.4.1 发布（2026-04-02）—— 插件稳定性补丁
**更新优先级**：🟡 中等

**核心修复**：
1. **频道插件兼容性修复**：旧版 `channels.*` 配置在限制性插件允许列表下无法加载捆绑频道插件的问题已修复
2. **插件运行时依赖恢复**：v2026.3.31 外部化变更导致捆绑插件丢失声明的运行时依赖，已恢复
3. **中文搜索大幅改善**：CJK BM25 查询不再在语义搜索前被重写，混合/空格分隔的中文查询准确性显著提升
4. **ClawHub 插件安装修复**：过时 1.2.0 常量检查导致安装失败的问题已修复
5. **插件卸载命令修复**：`clawhub:` specs 和无版本包名卸载恢复正常

**更新命令**：
```bash
npm install -g openclaw@latest
# 或
openclaw update
# 更新后验证
openclaw doctor --non-interactive
```

---

### 📋 v2026.3.31 完整内容回顾（2026-03-31）—— 零信任安全元年

#### 🔒 安全五大核心变更（破坏性）
| 变更 | 旧行为 | 新行为 |
|------|--------|--------|
| 节点执行 | `nodes.run()` 可直接执行命令 | 统一到 `exec host=node`，职责分离 |
| 插件安装 | 危险代码扫描 warn | **fail closed**：critical 直接阻止安装 |
| Gateway 认证 | localhost 回环免检 | 所有调用必须带 Authorization: Bearer TOKEN |
| 节点配对 | 配对成功即暴露命令 | 配对后需额外**审批**才可执行命令 |
| Plugin SDK | `openclaw/extension-api/*` | 迁移至 `openclaw/plugin-sdk/*` |

#### 🤖 新功能一览
- **QQ Bot** 官方原生支持（首个国内社交平台）：多账号、SecretRef、斜杠命令、定时提醒、富媒体收发
- **后台任务系统重构**：ACP/子代理/定时任务统一 SQLite 账本管理，新增 `openclaw flows list|show|cancel`
- **MCP 远程服务器**：`mcp.servers` 支持 HTTP/SSE 远程地址 + 认证头
- **Matrix 增强**：historyLimit 历史上下文 / HTTP 代理 / 流式回复草稿
- **Slack**：exec 审批可在 Slack 内直接完成
- **WhatsApp**：emoji 反应功能
- **LINE**：图片/视频/音频文件发送

#### ⚠️ 升级注意
```bash
# 升级前检查
openclaw update

# 节点审批：配对成功后需额外审批步骤才能使用节点命令
# 插件安装：如遇 critical 阻止，使用 --dangerously-force-unsafe-install 强制安装
```

---

## 🔧 配置技巧

### 1. 升级到 v2026.4.1 后的验证清单
```bash
# 1. 检查版本
openclaw --version

# 2. 验证插件加载
openclaw doctor --non-interactive

# 3. 验证定时任务（flows）
openclaw flows list

# 4. 验证中文搜索（如果有 QMD/Memory）
openclaw logs | grep -i "qmd\|search\|memory"
```

### 2. v2026.3.31 零信任安全配置（必做）
```bash
# 确保 Gateway 令牌已配置
openclaw config get | grep -A5 "auth"

# 如使用钉钉/企业微信，确认 compatibilityMode 兼容模式开启
# 检查 tools.profile
openclaw config get tools.profile
# 建议：coding

# 检查插件 allowlist
openclaw config get tools.allow
# 如果之前有自定义白名单，需重新评估
```

### 3. QQ Bot 接入（新功能，尝鲜推荐）
```bash
# 安装 QQ Bot 插件（v2026.3.31+ 内置）
openclaw plugins install qq

# 配置 ~/.openclaw/openclaw.json
{
  "channels": {
    "qq": {
      "enabled": true,
      "accounts": [
        {
          "id": "main",
          "botToken": "YOUR_BOT_TOKEN"
        }
      ]
    }
  }
}

openclaw gateway restart
```

### 4. 后台任务 Flows 管理（v2026.3.31+ 新命令）
```bash
# 查看所有运行中/历史任务
openclaw flows list

# 查看具体任务详情
openclaw flows show <flow-id>

# 取消任务
openclaw flows cancel <flow-id>

# 孤立任务自动修复
openclaw doctor --non-interactive
```

### 5. MCP 远程服务器配置（新功能）
```bash
# 在 openclaw.json 中配置远程 MCP
{
  "mcp": {
    "servers": {
      "remote-server": {
        "url": "https://your-mcp-server.com/sse",
        "headers": {
          "Authorization": "Bearer YOUR_TOKEN"
        }
      }
    }
  }
}
```

---

## ❓ 常见问题与解决

### Q1：插件安装报 critical 安全问题被阻止
**A**：这是 v2026.3.31 新增的 fail-close 策略。
```bash
# 绕过方法（⚠️ 谨慎使用）
openclaw plugins install <plugin-name> --dangerously-force-unsafe-install

# 推荐：先用 skill-vetter 扫描
clawhub install skill-vetter
npx clawhub@latest scan <plugin-name>
```

### Q2：升级后节点命令不工作
**A**：v2026.3.31 节点配对后需额外**审批步骤**。
- 检查网关审批：`openclaw gateway status`
- 手动触发审批：`openclaw nodes approve <node-id>`

### Q3：ClawHub 插件安装失败（报 1.2.0 常量错误）
**A**：这是 v2026.3.28 前遗留 bug，**v2026.4.1 已修复**，升级即可：
```bash
openclaw update
```

### Q4：QQ Bot 怎么接入？
**A**：v2026.3.31 内置了 QQ Bot 支持（长连接方式），参考上方配置技巧第 3 条。字节跳动已于 2026-04-01 上线 ClawHub 中国镜像，国内用户访问速度大幅提升。

### Q5：Cron 任务变成"孤立任务"了
**A**：`openclaw flows list` 查看孤立任务，`openclaw doctor --non-interactive` 自动修复关联。

---

## 🚀 最佳实践

### 1. 安全加固四步走（v2026.3.31 必做）
```bash
# ① 立即更新
openclaw update

# ② 运行安全审计
openclaw doctor --security

# ③ 轮换设备令牌
openclaw config set gateway.deviceTokenRotation true
openclaw gateway restart

# ④ 检查插件 allowlist
# 如果 tools.allow 有自定义，审查是否需要收紧
```

### 2. 多 Agent 协作推荐架构（2026-04 最新）
```
推荐 Skills（按实用性排序）：
⭐ self-improving-agent（记忆优化）
⭐ agent-council（多 Agent 协调）
  tavily-search（实时搜索）
  agent-browser（浏览器自动化）
  skill-vetter（安全扫描）

# 一键安装
npx clawhub@latest install self-improving-agent
npx clawhub@latest install agent-council
npx clawhub@latest install skill-vetter
```

### 3. Docker 部署最新配置
```yaml
# docker-compose.yml（v2026.4.1 兼容）
services:
  openclaw:
    image: openclaw/openclaw:latest
    environment:
      OPENCLAW_GATEWAY_PORT: "18789"
      OPENCLAW_TOOLS_PROFILE: "coding"
    ports:
      - "18789:18789"
    volumes:
      - ~/.openclaw:/root/.openclaw
    restart: unless-stopped
```

### 4. 钉钉/企业微信接入对比（2026-04 更新）

| 平台 | 接入方式 | 主动消息 | 文档读取 | 最新状态 |
|------|---------|---------|---------|---------|
| **钉钉** | 长连接 / webhook | ✅ 支持 | ✅ 私聊支持 | 稳定 |
| **企业微信** | 长连接（插件） | ✅ 支持 | ⚠️ 群聊不支持 | 稳定 |
| **QQ** | 长连接（内置） | ✅ 支持 | ✅ 支持 | **新功能** |

### 5. 版本选择建议
- **生产环境**：v2026.4.1（最新稳定，已修复 v2026.3.31 插件兼容性问题）
- **尝鲜用户**：可升级 v2026.4.1 体验 QQ Bot 和 Flows 系统
- **Node 版本**：推荐 Node 24（文档最新推荐），最低 Node 22.14+

---

## 📅 本期总结

**本周重大事件**：
1. 🔥 **v2026.4.1**（04-02）发布，插件兼容性 + 中文搜索改善
2. 🤖 **v2026.3.31**（03-31）"零信任安全元年"—— QQ Bot + SQLite 后台任务 + 五大安全收紧
3. 🇨🇳 **字节跳动 ClawHub 中国镜像** 04-01 上线，国内访问速度大幅提升
4. ⚠️ 安全警钟：CVE-2026-28466（CVSS 9.4）、CVE-2026-25253（CVSS 9.8）等多个高危漏洞持续活跃

**本周必做**：
```bash
openclaw update && openclaw doctor --security
openclaw flows list  # 体验新版任务控制面板
```

**资源链接**：
- 官方文档：https://docs.openclaw.ai/zh-CN
- 中文社区：https://clawd.org.cn
- ClawHub：https://clawhub.com
- 中国镜像：https://modelscope.cn/openclaw（如已上线）

---
*OpenClaw 配置周刊 · 由 OpenClaw 配置专家 Agent 自动生成 · 2026-04-03 22:30*

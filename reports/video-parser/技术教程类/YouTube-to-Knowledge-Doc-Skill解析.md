# 技术教程类 - YouTube-to-Knowledge-Doc Skill 完整解析

> 更新日期：2026-04-11
> 来源：https://skillsmp.com/zh/skills/tekliner-improvado-agentic-frameworks-and-skills-skills-youtube-to-knowledge-doc-skill-md

---

## 核心工具/API

| 工具 | 用途 | 安装/获取方式 |
|------|------|-------------|
| **yt-dlp** | YouTube字幕提取（自动字幕+指定语言） | `brew install yt-dlp`（macOS）|
| **get_session_id.py** | 获取 Claude Code 会话ID（来源追溯） | 项目内置脚本 |
| **youtube_to_notion.py** | 可选：上传到 Notion | 项目内置库（非临时脚本）|
| **Python** | 运行脚本 | `~/project/claude_venv/bin/python` |

---

## 步骤流程

### Step 1：提取视频内容

**① 下载字幕：**
```bash
yt-dlp --skip-download --write-auto-sub --sub-lang en --sub-format vtt \
  --output "/tmp/video_title" "YOUTUBE_URL"
```

**② 清理和去重字幕：**
```bash
cat /tmp/video_title.en.vtt | \
  grep -v "^WEBVTT" | grep -v "^Kind:" | grep -v "^Language:" | \
  grep -v "^[0-9][0-9]:" | grep -v "^$" | \
  sed 's/<[^>]*>//g' | \
  awk '!seen[$0]++' > /tmp/video_clean.txt
```

**③ 提取视频元数据：**
```bash
yt-dlp --skip-download --print "%(title)s|%(description)s|%(upload_date)s" "YOUTUBE_URL"
```

---

### Step 2：分析内容并确定文件夹

根据内容类型自动定位存放目录：

| 内容类型 | 存放路径 |
|---------|---------|
| AI Agent 开发/研究 | `algorithms/product_div/Ai Agent/` |
| 架构/设计模式 | `algorithms/product_div/00_Architecture/` |
| 市场研究/行业 | `algorithms/product_div/market_research/` |
| 多智能体系统 | `algorithms/product_div/Multi_agent_framework/` |
| 个人学习/探索 | `algorithms/A8_G&A_div/Daniel Personal/` |

> ⚠️ 先询问用户确认文件夹位置，再创建文件。

---

### Step 3：生成 Knowledge Framework 文档

**必需结构（MECE原则）：**

```
Thesis（论文）
  └─ 一句话预览所有MECE部分及具体成果

Overview（总览）
  └─ 介绍每个主要部分 + 主要来源 + 行内引用

Mermaid 图表
  └─ 复杂主题需同时包含：
     - Continuant (TD) 拓扑图
     - Occurrent (LR) 流程图

MECE 部分（1.0 / 2.0 / 3.0...）
  └─ 每个部分内使用 ¶1, ¶2, ¶3 段落编号

来源归属
```

**获取 Claude Code 会话ID（必需）：**
```bash
SESSION_ID=$(~/project/claude_venv/bin/python \
  ~/project/data_sources/claude_code/get_session_id.py --quiet)
```

**YouTube 来源归属模板：**
```markdown
**Primary source:** [Video Creator] "[Video Title]" YouTube (YYYY-MM-DD) [URL]
**Transcript extracted:** yt-dlp (YYYY-MM-DD)
**Generated by:** Claude Code session `SESSION_ID` by Daniel Kravtsov (YYYY-MM-DD)
```

**可点击时间戳引用（必需）：**
```markdown
**Source:** [Video timestamp 23:11-23:32](https://youtu.be/VIDEO_ID?t=1391)
**Quote:** "Direct quote from video" ([timestamp 23:11](https://youtu.be/VIDEO_ID?t=1391))
```
> 时间戳计算：`MM:SS → (MM × 60) + SS = SECONDS`（例：23:11 → 1391秒）

---

### Step 4：上传 Notion（可选）

> ❌ 禁止创建临时 Python 脚本，必须使用内置库：
```python
from data_sources.notion.youtube_to_notion import create_youtube_notion_page

create_youtube_notion_page(
    page_id="abc123",
    video_id="DxL2HoqLbyA",
    video_title="Entropy and Life",
    content_segments=[...],
    language="zh",
    session_id="a725304d-8bb9-42d5-9a0e-95258889f959"
)
```

---

### Step 5：文件命名规范

```bash
ls -1 algorithms/product_div/Ai\ Agent/ | grep "^[0-9][0-9]_" | tail -1
# → 创建：NN_key_topic_from_video.md
# 例如：02_anthropic_introspection_research.md
```

---

## 适用场景

- ✅ 将 YouTube 视频知识保存到结构化知识仓库
- ✅ 将外部学习资料转换为内部知识库（Notion/Obsidian）
- ✅ 技术分享/AI Agent 研究的结构化笔记
- ✅ 需要完整来源归属的学术/专业文档
- ✅ 建立个人/团队知识图谱

---

## 避坑指南

| 问题 | 解决方案 |
|------|---------|
| 字幕为空（YouTube 无自动字幕） | 使用 `--sub-lang zh-Hans,en` 多语言尝试，或手动上传字幕 |
| 时间戳不是可点击链接 | 必须用 `?t=SECONDS` 格式生成完整 URL |
| 文件夹位置错误 | Step 2 先确认再创建，禁止未确认就创建 |
| 缺少 Mermaid 图表 | 复杂主题必须同时包含 TD 和 LR 两种图表 |
| 创建临时脚本上传 Notion | ❌ 必须使用 `youtube_to_notion.py` 库 |
| 文档缺少会话ID | 无法追溯来源，不符合规范 |
| 扁平结构代替 MECE | 必须有层级编号（1.0/2.0/3.0 + ¶1/¶2/¶3）|

---

## 快速启动检查清单

```
[ ] 1. yt-dlp 提取字幕 (--write-auto-sub)
[ ] 2. 获取视频元数据（标题、描述、上传日期）
[ ] 3. 分析内容并建议文件夹位置
[ ] 4. 确认文件夹后创建文件
[ ] 5. 获取 Claude Code 会话ID (get_session_id.py --quiet)
[ ] 6. 生成 Knowledge Framework 文档（Thesis, Overview, Mermaid, MECE）
[ ] 7. 包含可点击时间戳链接 ([timestamp MM:SS](URL?t=SECONDS))
[ ] 8. [可选] 使用 youtube_to_notion.py 上传到 Notion
[ ] 9. 文档末尾添加作者检查清单
```

---

## 参考链接

- 技能主页：https://skillsmp.com/zh/skills/tekliner-improvado-agentic-frameworks-and-skills-skills-youtube-to-knowledge-doc-skill-md
- yt-dlp：https://github.com/yt-dlp/yt-dlp
- MECE 原则：https://en.wikipedia.org/wiki/MECE_principle
- Claude Code：https://claude.ai/code
